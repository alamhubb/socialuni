import UniProviderType from '@/plugins/uniapp-sdk/constant/UniProviderType'

export default class SocialLoginService {
  /**
   * 渠道登录的同一方法
   */
  static async providerLogin (provider?: UniProviderType) {
    /*UniUtil.showLoading('登录中')
    try {

    } finally {
      UniUtil.hideLoading()
    }*/
  }
}
