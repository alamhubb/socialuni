package com.socialuni.social.common.sdk.utils;


/*public class UserImgVOUtil {

    public static UserImgVO DOtoVO(UserImgDO userImg) {
        UserImgVO imgVO = new UserImgVO();
        imgVO.setId(userImg.getId().toString());
        imgVO.setSrc(userImg.getSrc());
        imgVO.setAspectRatio(userImg.getAspectRatio());

        imgVO.setWidth((double) 360);
        imgVO.setHeight(imgVO.getWidth() / imgVO.getAspectRatio());

        imgVO.setReportNum(userImg.getId());
        return imgVO;
    }

    public static List<UserImgVO> userImgDOToVOS(List<UserImgDO> imgDOs) {
        return imgDOs.stream().map(UserImgVOUtil::DOtoVO).collect(Collectors.toList());
    }
}*/
