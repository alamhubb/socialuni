export interface SocialuniStorageUtilInterface {
    setObj(key: string, value: any)

    getObj(key: string): any

    set(key: string, value: any): any

    get(key: string): any

    remove(key: string)
}
